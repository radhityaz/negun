name: exam_app_offline
description: Offline-first exam application for students.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  http: ^1.1.0
  crypto: ^3.0.3
  flutter_secure_storage: ^9.0.0
  # connectivity_plus: ^5.0.0 # For connection check
  # wakelock_plus: ^1.1.0 # Keep screen on

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
